# cocohusk

Storage repository for the HuskTech app's model files.

This repository holds no application source code. Its only purpose is to host the TensorFlow Lite model, labels, and rejection config as GitHub Release assets, so the HuskTech Android app has a stable, public URL to download them from at runtime.

Do not delete or rename files in existing releases, the app downloads them by exact filename.
